# Copy to .env and fill in your values.
# Used by Vite (GEMINI_API_KEY) and the Nebula Assistant.

GEMINI_API_KEY=

# Optional: API/WS origins for Mixed Content and HTTPS/WSS support
# Default: same-origin (uses page protocol - https/wss when page is HTTPS)
# VITE_API_ORIGIN=https://your-api.example.com
# VITE_WS_ORIGIN=wss://your-api.example.com

# Optional: Dev proxy target (Vite only, for /api/* routes)
# VITE_STATUS_API_TARGET=http://127.0.0.1:5050
